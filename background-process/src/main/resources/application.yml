spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/weather
    username: weather
    password: weather
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081